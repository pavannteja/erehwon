<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/" style="display: flex; align-items: center;">
        <img src="/images/logo-tranparent.png" alt="Erehwon Logo" style="height: 32px; width: auto;">
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/">Home</a>
          </li>
          <% if (currentUser) { %>
          <li class="nav-item">
            <a class="nav-link" href="/corporate-problems">Corporate Problems</a>
          </li>
          <% } %>
          <% if (currentUser && currentUser.isAdmin) { %>
          <!-- Admin users only see Home and Build -->
          <li class="nav-item">
            <a class="nav-link" href="https://orbitshift.frappe.cloud/lms/courses?">Build</a>
          </li>
          <% } else if (currentUser && !currentUser.isAdmin) { %>
          <!-- Non-admin logged-in users -->
          <li class="nav-item">
            <a class="nav-link" href="https://orbitshift.frappe.cloud/lms/courses?">Build</a>
          </li>
          <% } else { %>
          <!-- Logged-out users see Build only -->
          <li class="nav-item">
            <a class="nav-link" href="https://orbitshift.frappe.cloud/lms/courses?">Build</a>
          </li>
          <% } %>
        </ul>
        <div> 
          <div class="navbar-nav ml-auto">
            <% if (currentUser) { %>
            <% if (currentUser.isAdmin) { %>
            <!-- Admin users see Admin button and Logout -->
            <a class="nav-link" href="/admin/dashboard">
              <i class="fas fa-user-shield"></i> Admin
            </a>
            <a class="nav-link" href="/logout">Logout</a>
            <% } else { %>
            <!-- Non-admin users see Dashboard and Logout -->
            <a class="nav-link" href="/dashboard">
              <i class="fas fa-tachometer-alt"></i> Dashboard
            </a>
            <a class="nav-link" href="/logout">Logout</a>
            <% } %>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </nav>